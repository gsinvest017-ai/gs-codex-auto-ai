# 使用者專案產出（每人不同）— 但 v2 框架程式碼/測試保留進版控
src/*
!src/codexautoai_v2/
tests/*
!tests/codexautoai_v2/
!tests/tools/
!tests/test_updater.py
!tests/test_codex_runner.py
!tests/test_global_overlay.py
!tests/test_launcher.py
!tests/test_conpty.py
!tests/test_termserver.py
# ⚠️ 新增 tests/ 底下的**頂層**測試檔時，一定要在這裡加一條 `!tests/<檔名>`。
# 否則 `git add -A` 會靜默略過它，測試進不了版控也進不了 CI（看起來全綠但根本沒跑）。
# 子目錄 tests/codexautoai_v2/ 與 tests/tools/ 已整體放行，放那裡不必改本檔。
log/

# 環境與依賴
.venv/
node_modules/
__pycache__/
*.pyc

# 環境初始化紀錄（每人不同）
init.md

# docs 下的任務產出（每次不同），只保留 templates、codexAutoAI 框架文件與 guide 教學圖
docs/*
!docs/templates/
!docs/codexAutoAI-*.md
!docs/guide/

# 系統檔案
.DS_Store
Thumbs.db
*.swp
*.swo

# IDE
.vscode/
.idea/

# AGENTS.md 由 CLAUDE.md 生成（tools/sync_agents_md.py）並納入版控，供 Codex 讀取；
# 改指令一律改 CLAUDE.md（project.md C3 SSOT）。.agents/ 是工具的本地鏡像，不入庫。
.agents/

# 依賴鎖定檔（由各專案自行產生）
requirements.txt
package-lock.json

# 桌面 App 打包產物（原始碼與 desktop/codexautoai.ico 入庫；build 產物不入庫）
build/
dist/
*.spec
installer/payload/
desktop/__pycache__/

# VS Code extension 打包產物（原始碼入庫；框架快照/icon/vsix/node_modules 不入庫）
vscode-extension/framework/
vscode-extension/icon.png
vscode-extension/spec_forge_snapshot/
vscode-extension/node_modules/
*.vsix
