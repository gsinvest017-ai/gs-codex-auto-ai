# 使用者專案產出（每人不同）— 但 v2 框架程式碼/測試保留進版控
src/*
!src/codexautoai_v2/
tests/*
!tests/codexautoai_v2/
log/

# 環境與依賴
.venv/
node_modules/
__pycache__/
*.pyc

# 環境初始化紀錄（每人不同）
init.md

# docs 下的任務產出（每次不同），只保留 templates 與 codexAutoAI 框架文件
docs/*
!docs/templates/
!docs/codexAutoAI-*.md

# 系統檔案
.DS_Store
Thumbs.db
*.swp
*.swo

# IDE
.vscode/
.idea/

# AGENTS.md 由 CLAUDE.md 生成（tools/sync_agents_md.py）並納入版控，供 Codex 讀取；
# 改指令一律改 CLAUDE.md（project.md C3 SSOT）。.agents/ 是工具的本地鏡像，不入庫。
.agents/

# 依賴鎖定檔（由各專案自行產生）
requirements.txt
package-lock.json
