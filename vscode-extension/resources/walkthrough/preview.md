# ④ 看成果（內嵌預覽）

如果做的是有網頁畫面的東西，跑完後按控制台的 **🌐 即時預覽網頁 UI**——

會直接在 VS Code 裡**內嵌開啟**你的成品，不用自己開瀏覽器、不用自己啟動 server。

其他小提醒：

- **分工證據卡**：讓你看到 Claude 負責規劃、Codex 負責寫碼（誰花了多少）。
- **顯示背景終端機**：想看細節時的逃生口，平常不用理它。

就這樣——描述、啟動、看結果。祝順利 🎉
